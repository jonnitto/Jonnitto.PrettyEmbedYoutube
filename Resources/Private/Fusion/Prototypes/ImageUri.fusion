prototype(Jonnitto.PrettyEmbedYoutube:ImageUri) < prototype(ImageUri) {
    asset = ${q(node).property('customPreviewImage')}
    maximumWidth = ${maximumWidth}
    @if.hasPicture = ${q(node).property('customPreviewImage') != null}
}
